# Vault PostgreSQL storage config — generated by Ansible
# Sourced by fetch-postgres-password.sh at service start

GCP_PROJECT_ID="{{ gcp_project_id }}"
POSTGRES_SECRET_NAME="{{ postgres_secret_name }}"
POSTGRES_HOST="{{ postgres_host }}"
POSTGRES_PORT="{{ postgres_port }}"
POSTGRES_USER="{{ postgres_user }}"
POSTGRES_DATABASE="{{ postgres_database }}"
POSTGRES_SSLMODE="{{ postgres_sslmode }}"
POSTGRES_TABLE="{{ postgres_table }}"
POSTGRES_HA_ENABLED="{{ postgres_ha_enabled }}"
POSTGRES_HA_TABLE="{{ postgres_ha_table }}"